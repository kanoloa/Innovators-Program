name: MazdaInnovatorsProgram
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Check zip
      run: whcih zip

    - name: Check directory
      run: ls -al
      
